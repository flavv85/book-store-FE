export class Book {
  id: number;
  author: string;
  name: string;
  picByte: string;
  price: string;
  displayImg: string;
  // check if an item is added to the shopping cart
  isAdded: boolean;
}
